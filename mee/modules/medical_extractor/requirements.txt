# 医疗电子病历数据提取系统 - 依赖列表

# ============================================
# 核心依赖（必需）
# ============================================
PyQt5>=5.15.0
pandas>=1.3.0
openpyxl>=3.0.0
requests>=2.26.0

# ============================================
# 文件格式支持（可选）
# ============================================

# Word文档支持 (.docx) - 推荐安装
python-docx>=0.8.11

# PDF文件支持 (.pdf) - 推荐安装
PyPDF2>=3.0.0

# HTML/XML解析支持 (.html, .htm, .xml)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# RTF文件支持 (.rtf)
striprtf>=0.0.26

# OpenDocument支持 (.odt)
odfpy>=1.4.1

# ============================================
# 高级功能（可选）
# ============================================

# 旧版Word文档支持 (.doc) - 安装复杂，不推荐
# textract>=1.6.5

# ============================================
# 安装说明
# ============================================
#
# 1. 安装所有依赖（推荐）：
#    pip install -r requirements.txt
#
# 2. 仅安装核心依赖：
#    pip install PyQt5 pandas openpyxl requests
#
# 3. 添加常用格式支持：
#    pip install python-docx PyPDF2
#
# 4. 完整安装：
#    pip install -r requirements.txt
#
# ============================================
